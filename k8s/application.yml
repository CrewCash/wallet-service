spring:
  datasource:
    url: jdbc:postgresql://crewcash-postgres/crewcashdb_wallet
  security:
    oauth2:
      resourceserver:
        jwt:
          issuer-uri: http://crewcash-keycloak/realms/CrewCash
